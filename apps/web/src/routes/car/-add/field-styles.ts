// Shared Tailwind class strings for the add-car form fields, so the section
// components render visually identical inputs and labels.
export const inputClass =
    "w-full rounded-xl border border-border bg-input text-text-primary px-3 py-3 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-primary/10 transition-colors  appearance-none";

export const labelClass = "text-sm font-bold text-text-primary mb-1 block";
